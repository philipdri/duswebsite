DATABASE_URL="postgresql://user:password@host/database?sslmode=verify-full"
ADMIN_PASSWORD="your-secure-admin-password"
ADMIN_SESSION_SECRET="your-random-32-plus-character-session-secret"
# Required for image uploads in production (Vercel Blob).
# Create a Blob store in the Vercel dashboard and paste the token here.
# Leave unset to use local filesystem storage in development.
BLOB_READ_WRITE_TOKEN=""
